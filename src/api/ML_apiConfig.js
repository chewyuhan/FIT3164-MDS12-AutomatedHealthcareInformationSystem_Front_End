const local = "http://localhost:5000";
const host = "https://mds12htr.cyclic.app";

// Change this to local if you want to run the app locally
const BASE_URL = host;

export const IMAGES_API = {
    UPLOAD_SAMPLE: `${BASE_URL}/patient_htr_sample`,
};
